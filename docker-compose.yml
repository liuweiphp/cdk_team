services:
  mysql:
    container_name: exc_mysql
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD:?DB_ROOT_PASSWORD is required (set it in .env)}
      MYSQL_DATABASE: exchange_cdk
    ports:
      - "3307:3307"
    volumes:
      - ./data/mysql:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 5s
      timeout: 3s
      retries: 10
    networks:
      - exc-network

  backend:
    container_name: exc_backend
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      DB_DSN: root:${DB_ROOT_PASSWORD:?DB_ROOT_PASSWORD is required (set it in .env)}@tcp(mysql:3306)/exchange_cdk?charset=utf8mb4&parseTime=True&loc=Local
      SERVER_PORT: 8080
    ports:
      - "8081:8080"
    depends_on:
      mysql:
        condition: service_healthy
    networks:
      - exc-network

  frontend:
    container_name: exc_frontend
    image: nginx:alpine
    volumes:
      - ./frontend/dist:/usr/share/nginx/html:ro
      - ./frontend/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    ports:
      - "3000:80"
    depends_on:
      - backend
    networks:
      - exc-network

networks:
  exc-network:
